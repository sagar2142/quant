"""Factor research — the fast loop.

Scores a signal directly against forward returns instead of inferring its
value from an equity curve, then combines signals into a composite. Most ideas
should die here; only survivors are worth a backtest and the gauntlet.
"""
